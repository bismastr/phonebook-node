{
  "name": "phonebook-node",
  "version": "1.0.0",
  "description": "Phonebook node",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "dev": "nodemon index.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "build:ui": "rm -rf dist && cd ../bisma-learning-fullstack/part2/phonebook && npm run build && cp -r dist ~/Documents/learning/phonebook-node",
    "deploy:full": "npm run build:ui && git add . && git commit -m uibuild && git push"
  },
  "author": "bismastr",
  "license": "ISC",
  "devDependencies": {
    "@flydotio/dockerfile": "^0.5.4",
    "nodemon": "^3.1.0"
  },
  "dependencies": {
    "express": "^4.19.2",
    "mongoose": "^8.3.1",
    "morgan": "^1.10.0"
  }
}
